# PDF 护眼模式 Edge 扩展

## 需求概述

设计一个 Microsoft Edge 浏览器扩展，当用户打开 PDF（在线或本地）时，自动将 PDF 以护眼色调展示，减少白色背景对眼睛的刺激。

## 核心问题

Edge 内置 PDF 阅读器受沙箱保护，扩展无法直接修改其页面样式。需要绕过 Edge 自带的 PDF 阅读器，用自定义页面加载和渲染 PDF。

## 参考实现

Edge 扩展 `extension://oikmahiipjniocckomdccmplodldodja`（PDF 翻译扩展）已实现了类似能力：
- 能识别当前页面是 PDF
- 在新页面中打开可翻译的 PDF

本扩展需要实现类似的 PDF 识别 + 新页面打开能力，只是打开后不是翻译，而是应用护眼色调。

## 功能要求

### 1. PDF 识别与拦截
- 检测用户正在访问 PDF 文件（URL 以 `.pdf` 结尾，或 Content-Type 为 `application/pdf`）
- 拦截 PDF 的加载，将其重定向到自定义阅读页面
- 支持在线 PDF 链接和本地 PDF 文件

### 2. 自定义 PDF 阅读器
- 使用 Mozilla PDF.js 渲染 PDF（可通过 CDN 加载）
- 远程 PDF 数据需解决跨域问题（由 background script 代理获取后传给阅读页面）
- 支持翻页、缩放（放大/缩小/适合宽度）、页码显示
- 工具栏可折叠

### 3. 护眼模式
通过 CSS filter + 背景色实现，支持以下模式：

| 模式 | CSS Filter | 背景色 |
|------|-----------|--------|
| 暖色调 | `sepia(0.15) brightness(0.95) saturate(0.9)` | `#f5e6c8` |
| 牛皮纸 | `sepia(0.4) brightness(0.92) contrast(1.05)` | `#f4ecd8` |
| 深色模式 | `invert(0.88) hue-rotate(180deg) brightness(0.9)` | `#1a1a2e` |
| 绿色护眼 | `sepia(0.1) hue-rotate(60deg) brightness(0.95) saturate(0.7)` | `#c7edcc` |

- 阅读器顶部提供模式切换下拉框
- 用户可在工具栏按钮快速轮换模式

### 4. 扩展 Popup
- 开关：启用/禁用护眼模式（禁用时 PDF 按正常方式打开）
- 默认模式选择

### 5. 设置持久化
- 使用 `chrome.storage.local` 存储启用状态和模式偏好

## 技术约束

- Manifest V3
- 需处理 CORS 问题：扩展 background 可跨域 fetch，页面内不行
- 需要的权限：`activeTab`、`storage`、`tabs`、`<all_urls>` host permission
